apiVersion: v1
kind: Service
metadata:
  name: {{ include "article.fullname" . }}
  labels:
    {{- include "article.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "article.selectorLabels" . | nindent 4 }}
